convert string to number : 
~
let theNumber = Number(prompt("Pick a number"));
if (!Number.isNaN(theNumber)) {
console.log("Your number is the square root of " +
theNumber * theNumber);
}
~
support shorcuts :
counter += 1;
counter++ 

Switch:
case:
default:

use break in everyCase

always user functionName() if fn like Number() is used , it means its contructor.



